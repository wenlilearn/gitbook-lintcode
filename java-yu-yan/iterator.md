# Iterator

```java
Iterator iterator = something.iterator() // will return an iterator
// Make sure Iterator has next
while(iterator.hasNext()) {
    //...do something...
    iterator.next(); // extracts the next value in iterator
}
```

